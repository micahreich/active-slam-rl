[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "asrl"
version = "0.1.0"
description = "An extremely basic example package"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Micah Reich", email = "mreich@cmu.edu" }
]
license = { file = "LICENSE" }
dependencies = []
